{
  "srvcId": "${artifactId}-${version}",
  "nodeId": "localhost",
  "descriptor": {
    "exec": "java -Dport=%p -jar ../mod-locations/mod-locations-server/target/mod-locations-fat.jar -Dhttp.port=%p"
  }
}
