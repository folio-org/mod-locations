repos:
  - repo: local
    hooks:
      - id: lint-api
        name: API lint (Spectral)
        language: script
        entry: scripts/lint-api.sh
        pass_filenames: false
        stages: [pre-push]
